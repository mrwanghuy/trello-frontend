# TrelloLite Frontend

Next.js 14 + TypeScript + Tailwind + shadcn-style UI + dnd-kit.

## Prereqs

- Node.js >= 20
- Backend NestJS chạy tại `http://localhost:3001`

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Mở `http://localhost:3000`.

## Login demo

- Email: `demo@trellolite.dev`
- Password: `demo1234`

## Scripts

- `npm run dev` — dev server
- `npm run build` — production build
- `npm run start` — chạy bản build
- `npm run lint` — ESLint
- `npm run type-check` — TypeScript check
